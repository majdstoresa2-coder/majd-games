{
  "name": "Majd-Automation-Security-Core",
  "version": "2.0",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "majd-secure-webhook",
        "responseMode": "responseNode",
        "options": {}
      },
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [250, 300],
      "name": "Webhook Security Trigger"
    },
    {
      "parameters": {
        "jsCode": "// التحقق من سلامة البيانات الواردة للمنصة\nconst inputData = $input.all();\nreturn inputData.map(item => {\n  return {\n    json: {\n      status: \"verified\",\n      timestamp: new Date().toISOString(),\n      payload: item.json\n    }\n  };\n});"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [500, 300],
      "name": "Verify Payload Syntax"
    }
  ],
  "connections": {
    "Webhook Security Trigger": {
      "main": [
        [
          {
            "node": "Verify Payload Syntax",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  }
}
{
  "name": "automation",
  "version": "2.0",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "majd-secure-webhook",
        "responseMode": "responseNode",
        "options": {}
      },
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [250, 300],
      "name": "Webhook Security Trigger"
    },
    {
      "parameters": {
        "jsCode": "// التحقق من سلامة البيانات الواردة للمنصة\nconst inputData = $input.all();\nreturn inputData.map(item => {\n  return {\n    json: {\n      status: \"verified\",\n      timestamp: new Date().toISOString(),\n      payload: item.json\n    }\n  };\n});"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [500, 300],
      "name": "Verify Payload Syntax"
    }
  ],
  "connections": {
    "Webhook Security Trigger": {
      "main": [
        [
          {
            "node": "Verify Payload Syntax",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  }
}
